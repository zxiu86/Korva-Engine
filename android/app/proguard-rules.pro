# Add project-specific ProGuard/R8 rules here.
#
# Korva Engine currently has no reflection-based or JNI calls of its own
# that need explicit keep rules beyond the defaults Flutter and its
# plugins already ship with. Add rules here if you introduce a library
# that needs them (R8's missing_rules.txt, printed on a failed release
# build, will tell you exactly what's missing).
